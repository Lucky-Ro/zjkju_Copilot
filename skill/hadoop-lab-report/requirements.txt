# hadoop-lab-report 运行依赖(Python 3.10+)
paramiko>=3.0        # SSH 执行引擎(交互通道、持久 shell)
requests>=2.28       # 抓取教程页
beautifulsoup4>=4.11 # 解析教程 HTML
python-docx>=1.1     # 填学校 Word 模板

# 可选:用于把导出的报告 PDF 渲染成图做预览/自检
# pymupdf>=1.23

# 仅 Windows:模板 .doc→.docx 用系统自带 Word(COM)或 LibreOffice soffice;
# 终端截图用系统自带 Edge 无头模式 —— 均无需 pip 安装。
